nginx:
        build: ./nginx
        links:
                - server:server
        ports:
                - "80:80"
server:
        build: ./server
        ports:
                - "3000:3000"
        environment:
                - LDREPLAYSPEED=15
